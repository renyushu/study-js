/*
    package.json
        scripts:
            - 可以自定义一些命令
            - 定义以后可以直接通过npm来执行这些命令
            - start 和 test 可以直接通过 npm start npm test执行
            - 其他命令需要通过 npm run xxx执行

   npm镜像
       npm set registry https://registry.npmmirror.com

       还原到原本仓库
            npm config delete registry

 */